package info.scce.dime.config;

abstract class AbstractAppConfig implements AppConfigInterface {
    @Override
    public String getString(String key) throws KeyNotFoundException, ConfigNotValidException {
        return (String) get(key);
    }
}
